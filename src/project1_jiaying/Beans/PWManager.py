class PWManager:
        def __init__(self,pwd, description, URL, accnt, id = 0):
                self.accnt=accnt
                self.id = id
                self.pwd = pwd
                self.description = description
                self.URL = URL